#!/usr/bin/env bash
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"

bash "$ROOT/scripts/check-rust-file-size.sh" "$ROOT/bitloops_cli"
cargo fmt --all --manifest-path "$ROOT/bitloops_cli/Cargo.toml"
cargo clippy --manifest-path "$ROOT/bitloops_cli/Cargo.toml" --all-targets --all-features -- -D warnings
