2026-03-18

TestLens Stage 1 storage refactor progress

Status: complete

Completed
- Replaced the TestLens production schema with the bitloops_cli-compatible production tables.
- Split test data into dedicated tables: test_suites, test_scenarios, test_links, test_runs, test_classifications, coverage_captures, coverage_hits, test_discovery_runs, test_discovery_diagnostics.
- Refactored production ingestion to write repositories, commits, file_state, current_file_state, artefacts, and artefacts_current.
- Refactored test discovery/materialization to stop writing test suites and scenarios into production artefacts.
- Updated coverage, results, query, seed, and repository paths to use test_scenario_id and production_artefact_id.
- Updated e2e assertions and sqlite helpers to match the new schema split.
- Verified with cargo test in TestLens: passing.

Next updates
- Append new entries here as follow-up work happens.

Validation follow-up
- Ran the Ruff quickstart against a brand-new DB at `./TestLens/target/ruff-real-project-clear-20260318-v3.db`.
- Verified `init`, `ingest-production-artefacts`, `ingest-tests`, and workspace `ingest-coverage` all succeed on a clean-state DB.
- Current clean-run shape: production artefacts `15851`, test suites `904`, test scenarios `5034`, test links `67262`, coverage hits `71145`, enumeration `hybrid-partial` with `175` enumerated scenarios.
- Fixed a Rust production-ingest collision where trait impl methods with the same name collapsed to one symbol. Added regression test `keeps_distinct_trait_impl_methods_for_same_type`.
- Current Ruff production ingest still reports `normalized 3 duplicate artefact identities during persistence`; the run succeeds, but that remaining normalization note is worth a follow-up if exact one-to-one symbol accounting matters.
- Updated the Ruff quickstart and validation markdowns to match the split schema, current Ruff counts, and the validated workspace-LCOV flow.
- Compatibility note: reusing an old pre-split-schema Ruff DB is not supported. Start from a fresh DB file or delete the old DB first.

Cleanup
- Deleted all generated SQLite DB files under `./TestLens/target`.
- Verified there are no remaining `.db`, `.sqlite`, or `.sqlite3` files under the workspace.

Post-Stage-1 planning
- Updated `compatibility-plan.md` with the post-Stage-1 path, including the immediate duplicate-identity investigation and the Stage 2 goal of making `bitloops_cli` the runtime source of truth for production artefacts.
- Started the first follow-up step: tracing the remaining `3` normalized duplicate production artefact identities reported by Ruff production ingest.

Duplicate-identity investigation
- Traced the remaining `3` normalized duplicate artefact identities on Ruff.
- They are all cfg-gated alternate definitions rather than another broad semantic-identity bug:
  - `find_user_settings_toml` in `crates/ruff_workspace/src/pyproject.rs`
  - `SarifResult::from_message` in `crates/ruff_linter/src/message/sarif.rs`
  - `now` in `crates/ruff_db/src/system/memory_fs.rs`
- In all three cases, Ruff defines `wasm32` and non-`wasm32` implementations with the same semantic name/signature.
- Current conclusion: treat these `3` cases as acceptable normalization for now and keep the CLI note honest about them. Revisit cfg-aware identity only if Stage 2 integration with `bitloops_cli` requires stricter parity.
- Re-ran the `ingest_production_artefacts` test slice after adding the diagnostic hook: passing.

bitloops_cli fresh-DB ingest check
- Inspected the real `bitloops_cli` production-ingest flow in `bitloops_cli/src/commands/devql.rs`, `bitloops_cli/src/engine/devql/mod.rs`, and the DevQL persistence modules.
- Verified the real commands are `bitloops devql init` and `bitloops devql ingest`.
- Ran a fresh-store check using temporary DevQL store paths:
  - relational: `.bitloops/stores/relational/compat-check-20260318.db`
  - events: `.bitloops/stores/event/compat-check-20260318.duckdb`
  - env: `BITLOOPS_DEVQL_EMBEDDING_PROVIDER=disabled` and `BITLOOPS_DEVQL_SEMANTIC_PROVIDER=disabled`
- Result on this repo: `checkpoints_processed=0`, `events_inserted=0`, `artefacts_upserted=0`.
- Verified the fresh relational DB state after ingest:
  - `repositories=1`
  - `commits=0`
  - `file_state=0`
  - `current_file_state=0`
  - `artefacts=0`
  - `artefacts_current=0`
  - `artefact_edges=0`
  - `artefact_edges_current=0`
  - `checkpoints=0`
  - `checkpoint_blobs=0`
  - `commit_checkpoints=0`
- Key integration finding: `bitloops_cli` production ingest is checkpoint-driven, not repo-scan-driven. It replays committed checkpoint metadata and `files_touched` into production tables; without that checkpoint state, a fresh DB only gets the repository row.
- Additional operational finding: the default embedding provider is `local`, which bootstraps a cached Jina model under `.bitloops/embeddings/models` before checkpoint processing unless explicitly disabled.

Bitloops-backed Ruff quickstart draft
- Added `TestLens/docs/quickstart_ruff_bitloops_devql.md` to capture the intended Stage 2 Ruff flow:
  - Bitloops checkpoint creation in the Ruff repo
  - `bitloops devql ingest` for production artefacts
  - `testlens init` and `testlens ingest-tests` against the same relational DB
- Verified that `testlens init --db <bitloops relational db>` succeeds on a Bitloops-created SQLite database.
- Verified that the combined DB then contains both the DevQL production/runtime tables and the TestLens test-domain tables.
- Verified the current production-missing boundary on that combined DB with Ruff:
  - `testlens ingest-tests` fails fast with `no production artefacts found for commit ...`
  - the current error text still points at `testlens ingest-production-artefacts`, which should be updated once the Bitloops-backed path becomes the intended workflow

Checkpoint requirement re-check
- Re-ran the question directly on March 18, 2026 with a disposable git repo that had:
  - a source file
  - a normal git commit
  - Bitloops initialized and enabled
  - no Bitloops checkpoint rows
- Ran `BITLOOPS_DEVQL_EMBEDDING_PROVIDER=none bitloops devql init` and `BITLOOPS_DEVQL_EMBEDDING_PROVIDER=none bitloops devql ingest`.
- Result: `DevQL ingest complete: checkpoints_processed=0, events_inserted=0, artefacts_upserted=0`.
- Verified the relational DB state after ingest:
  - `repositories=1`
  - `checkpoints=0`
  - `commit_checkpoints=0`
  - `commits=0`
  - `artefacts_current=0`
- Conclusion: a plain git commit is not enough for production artefact materialization. For `bitloops devql ingest`, committed Bitloops checkpoint state is the gating input.
- Contrast check: on the disposable `testlens-fixture-rust` repo copy, `bitloops devql ingest` did materialize production rows only after committed checkpoint rows existed; that run reported `checkpoints_processed=1`, `events_inserted=1`, and `artefacts_upserted=2`.

cfg-if quickstart
- Added `quickstart.md` at the repo root with a copy-pasteable Bitloops + TestLens flow against `rust-lang/cfg-if`.
- The quickstart installs both CLIs from this workspace, clones `cfg-if` under `/tmp`, initializes Bitloops, creates a committed checkpoint, runs `bitloops devql ingest`, and then continues with `testlens init` and `testlens ingest-tests` against the same SQLite DB.
- Re-validated that flow on March 18, 2026 using a disposable `/tmp/cfg-if` clone.
- Verified the shell-driven checkpoint cycle with manual Codex hook calls:
  - `bitloops hooks codex session-start`
  - tracked source edit
  - `bitloops hooks codex stop`
  - `git commit`
- Verified Bitloops state on the disposable `cfg-if` clone after the validated cycles:
  - `checkpoints=2`
  - `commit_checkpoints=2`
- Verified DevQL production materialization on the same repo-local DB after ingest:
  - `repositories=1`
  - `commits=2`
  - `file_state=5`
  - `artefacts_current=10`
  - `artefact_edges_current=1`
- Verified TestLens continuation on the same DB:
  - `testlens init --db <cfg-if-db>` succeeded
  - `testlens ingest-tests --commit <head>` completed with `files: 2, suites: 4, scenarios: 5, links: 0, enumeration: hybrid-full, enumerated_scenarios: 2`

Schema parity check
- Re-verified the production-schema compatibility directly by diffing the production DDL in `TestLens/src/db/schema.rs` against `bitloops_cli/src/engine/devql/ingestion/schema/relational_sqlite_schema.rs`.
- Result: no diff across the production tables and indexes:
  - `repositories`
  - `commits`
  - `file_state`
  - `current_file_state`
  - `artefacts`
  - `artefacts_current`
  - `artefact_edges`
  - `artefact_edges_current`
- Conclusion: at the SQLite schema level, TestLens production storage mirrors `bitloops_cli` exactly.
- Small implementation note: the TestLens Rust domain layer is slightly stricter than the SQL schema in a few places, for example `ProductionArtefactRecord.symbol_id` and `canonical_kind` are modeled as required `String`s even though the underlying SQL schema allows nullable values in `artefacts`.

Agent guidance
- Updated `TestLens/AGENTS.md` with the production-compatibility boundary for ongoing work:
  - keep the production SQLite schema identical to `bitloops_cli`
  - keep test-domain state owned only by `TestLens`
  - make `TestLens` read production artefacts from DevQL tables for the real workflow
  - keep `testlens ingest-production-artefacts` as a local bootstrap/prototype path only

Ruff coverage status clarification
- Re-checked the current Ruff coverage story in the docs and query path.
- Current state for the main Ruff fixture (`75a24bbc67aa31b825b6326cfb6e6afdf3ca90d5`):
  - workspace LCOV ingestion is validated
  - the validated local run shape is `hits: 71145`
  - query output reports workspace coverage as `coverage_mode: artefact_only`
- Practical meaning:
  - TestLens can correctly tell whether a production artefact was exercised somewhere in the Ruff workspace coverage run
  - TestLens does not yet attribute that workspace coverage back to specific Ruff tests
  - linked tests on Ruff still come from static linkage unless isolated per-test captures are ingested

2026-03-19

bitloops_cli TestLens migration tranche

Status: implemented

Completed
- Migrated the core TestLens flow into `bitloops_cli` under a new `bitloops testlens` namespace:
  - `bitloops testlens init`
  - `bitloops testlens ingest-tests`
  - `bitloops testlens ingest-coverage`
  - `bitloops testlens query`
  - `bitloops testlens list`
- Kept DevQL production artefacts as the source of truth for the migrated path. The new Bitloops command flow reads production rows from the existing relational tables and only writes TestLens test-domain tables.
- Kept the standalone `testlens` binary in place as a temporary compatibility path. The legacy `ingest-production-artefacts` command was not moved into `bitloops_cli`.
- Refactored the shared TestLens core so `ingest-tests`, `ingest-coverage`, and query/list rendering can run against injected repositories instead of opening SQLite directly.
- Added a Bitloops-side repository layer for the migrated path:
  - SQLite reuses the existing `TestLens` repository implementation
  - Postgres now has a dedicated `TestHarnessRepository` / `TestHarnessQueryRepository` implementation in `bitloops_cli`
- Added an idempotent test-domain-only schema init path for SQLite in `TestLens`, so `bitloops testlens init` no longer needs to run the full prototype schema bootstrap.
- Added Postgres test-domain schema DDL in `bitloops_cli` for:
  - `test_suites`
  - `test_scenarios`
  - `test_links`
  - `test_runs`
  - `test_classifications`
  - `coverage_captures`
  - `coverage_hits`
  - `test_discovery_runs`
  - `test_discovery_diagnostics`
- Normalized `TestLens` onto `rusqlite 0.37` so it can be linked into `bitloops_cli` as a library dependency without `libsqlite3-sys` conflicts.
- Updated the missing-production error path to point at production materialization in general, with explicit Bitloops guidance.

Validation
- `cargo test` in `./TestLens`: passed.
- `cargo test init_schema_for_repo_initialises_sqlite_test_domain_tables` in `./bitloops_cli`: passed.
- `cargo test postgres_test_domain_schema_includes_core_tables` in `./bitloops_cli`: passed.
- Manual end-to-end validation on March 19, 2026 against a disposable git repo copied from `TestLens/testlens-fixture`:
  - seeded production rows with standalone `testlens ingest-production-artefacts` into a Bitloops-configured SQLite store
  - `bitloops testlens init`: succeeded
  - `bitloops testlens ingest-tests --commit b2f21a8595997058ef8bc3e10deecaca7e4c88a6`: succeeded with `files: 3, suites: 3, scenarios: 6, links: 20, enumeration: source-only`
  - `bitloops testlens list --commit ...`: returned production artefacts plus `test_suite` / `test_scenario` rows from the same relational DB
  - `bitloops testlens ingest-coverage --lcov coverage/lcov.info --scope workspace`: succeeded with `hits: 101`
  - `bitloops testlens query --artefact UserService.createUser --view summary`: returned `partially_tested`, `total_covering_tests: 3`, and `coverage_mode: artefact_only`
  - `bitloops testlens query --artefact UserService.createUser --view tests --min-strength 0.0`: returned the linked TypeScript scenarios as expected

Current scope boundary
- This tranche does not migrate:
  - `ingest-results`
  - `ingest-coverage-batch`
  - removal of the standalone `testlens` binary
  - Bitloops-backed production ingestion replacement for the prototype `testlens ingest-production-artefacts` command

2026-03-19

Revised SQLite init boundary and test-harness organization

Status: implemented

Completed
- Removed the temporary coupling from `bitloops init`; it now stays focused on store/bootstrap concerns only.
- Moved the internal Bitloops-side TestLens integration under `bitloops_cli/src/engine/test_harness`.
- Kept the command layer thin: `bitloops testlens ...` now routes into the test-harness module rather than owning the operational logic itself.
- Wired test-harness schema initialization into the DevQL relational init path instead of `bitloops init`, so the SQLite and Postgres relational schemas are created together by `bitloops devql init` (and by `devql ingest --init`).
- Kept `bitloops testlens init` as a compatibility path that calls the same test-harness schema initializer.
- Added and kept binary-facing SQLite acceptance coverage in `bitloops_cli/tests/testlens_sqlite_acceptance.rs` for the supported migrated surface:
  - `bitloops init`
  - `bitloops devql init`
  - `bitloops testlens ingest-tests`
  - `bitloops testlens list`
  - `bitloops testlens query`
  - `bitloops testlens ingest-coverage`
- Added an explicit smoke/acceptance assertion for the core ingest-tests contract:
  - given an initialized repository with production artefacts
  - when `bitloops testlens ingest-tests --commit ...` runs
  - then `test_suites`, `test_scenarios`, and `test_links` are created and the resulting production-artefact query returns linked test scenarios
- Converted that core ingest-tests smoke flow into Gherkin/Cucumber acceptance coverage in `bitloops_cli/tests/testlens_gherkin.rs` with feature text in `bitloops_cli/tests/features/test_harness/ingest_tests.feature`.
- Split the Gherkin smoke coverage by language so one scenario does not mix TypeScript and Rust:
  - one scenario validates the TypeScript ingest-tests flow and queryable links
  - one scenario validates the Rust ingest-tests flow and queryable links
- Consolidated the current test-harness Gherkin scenarios into a single descriptive feature file:
  - `bitloops_cli/tests/features/test_harness/structural_mapping.feature`
  - descriptive scenario names are preferred over stale ticket-coded names
- Kept the lower-level SQLite integration file focused on non-Gherkin checks that are still useful there:
  - schema-boundary behavior (`bitloops init` vs `bitloops devql init`)
  - workspace coverage ingestion and `coverage_mode: artefact_only`
- Kept the acceptance setup aligned with the intended merged shape:
  - `bitloops init` prepares stores and hooks only
  - `bitloops devql init` owns relational schema creation, including test-harness tables
  - the migrated TestLens path runs through `bitloops testlens ...`
  - production rows for the acceptance tests are bootstrapped through shared TestLens production-ingest code, not by invoking the standalone `testlens` binary

Validation
- `cargo test run_init_creates_default_store_databases_and_blob_directory` in `./bitloops_cli`: passed.
- `cargo test init_schema_for_repo_initialises_sqlite_test_harness_tables` in `./bitloops_cli`: passed.
- `cargo test init_relational_schema_creates_test_harness_tables` in `./bitloops_cli`: passed.
- `cargo test --test testlens_gherkin` in `./bitloops_cli`: passed with 2 scenarios / 11 steps after consolidating the feature file to `structural_mapping.feature`.
- `cargo test --test testlens_sqlite_acceptance` in `./bitloops_cli`: passed.
- The acceptance file validates three SQLite-first flows:
  - `bitloops init` does not create test-harness tables by itself
  - `bitloops devql init` creates the test-harness tables in the configured relational store
  - the Gherkin smoke flow validates `bitloops testlens ingest-tests` + `list` + `query` against DevQL-initialized schema plus shared production rows
  - `bitloops testlens ingest-coverage` reports workspace coverage as `coverage_mode: artefact_only`

Still pending
- Move or retire the remaining standalone-CLI acceptance coverage that still depends on unmigrated commands.
- Decide when to drop the now-redundant migrated-path acceptance cases from the `TestLens` crate once `bitloops_cli` becomes the only supported binary surface.

2026-03-19

Expanded Bitloops-side Gherkin structural mapping coverage

Status: implemented

Completed
- Grew `bitloops_cli/tests/features/test_harness/structural_mapping.feature` from the initial 2 smoke scenarios into the main structural/linkage acceptance spec for the migrated TestLens surface.
- Kept the feature file behavior-focused and descriptive instead of carrying over legacy ticket-coded scenario names.
- Ported the remaining Rust structural mapping scenarios that were still only living in `TestLens`:
  - inline parameterized source tests (`#[test_case(...)]`)
  - additional Rust test declarations (`wasm_bindgen_test`, quickcheck-style macro tests)
  - cargo-backed hybrid discovery (`rstest`, `proptest`, doctests)
- Moved the supporting fixture builders into `bitloops_cli/tests/test_harness_support/mod.rs`, so the binary-facing test harness owns the fixture setup for these acceptance flows.
- Extended `bitloops_cli/tests/testlens_gherkin.rs` so the Bitloops-side Cucumber runner can assert:
  - exact materialized scenario names for parameterized and additional-declaration fixtures
  - hybrid enumeration output from `bitloops testlens ingest-tests`
  - linkage/query expectations for `double`, `triple`, `documented_increment`, `render_message`, `is_equivalent_to`, and the Ruff-style rule harness functions

Validation
- `cargo test --manifest-path ./bitloops_cli/Cargo.toml --test testlens_gherkin`: passed.
- The updated Gherkin suite now runs 1 feature with 6 scenarios / 30 steps, all passing.
- `cargo test --manifest-path ./bitloops_cli/Cargo.toml --test testlens_sqlite_acceptance`: passed after the Gherkin expansion, so the schema-boundary and coverage assertions still hold.

Current migration gap
- The command surface and the main ingest-tests structural/linkage acceptance suite are migrated.
- The remaining acceptance migration work is now concentrated in the real-fixture quickstart and any query/coverage journey specs that still matter after ignoring `ingest-results`.

2026-03-19

Migrated `ingest-results` and `ingest-coverage-batch` into `bitloops testlens`

Status: implemented

Completed
- Refactored `TestLens/src/app/commands/ingest_results.rs` so it now exposes a repository-injected `execute()` path plus a summary printer, matching the migrated command pattern already used by `ingest-tests` and `ingest-coverage`.
- Refactored `TestLens/src/app/commands/ingest_coverage_batch.rs` so it now exposes a repository-injected `execute()` path and no longer depends on opening SQLite directly.
- Added the corresponding Bitloops-side command wrappers in `bitloops_cli/src/commands/testlens.rs`:
  - `bitloops testlens ingest-results --jest-json ... --commit ...`
  - `bitloops testlens ingest-coverage-batch --manifest ... --commit ...`
- Kept `ingest-production-artefacts` out of the migrated Bitloops command surface; it remains a prototype/bootstrap path in shared code only.
- Added Bitloops-side TypeScript runtime acceptance coverage under `bitloops_cli/tests/features/test_harness/runtime_signals.feature`.
- Added supporting fixture helpers in `bitloops_cli/tests/test_harness_support/mod.rs` to:
  - copy a lightweight real TypeScript fixture subset
  - write a deterministic Jest JSON result file
  - write a coverage-batch manifest
- Extended `bitloops_cli/tests/testlens_gherkin.rs` so the Bitloops-side Cucumber suite now verifies:
  - `ingest-coverage-batch` enriches summary and coverage query views with real percentages and branch entries
  - `ingest-results` surfaces failing `last_run` status in tests view for linked covering tests

Validation
- `cargo test --manifest-path ./bitloops_cli/Cargo.toml --test testlens_gherkin`: passed.
- The Bitloops-side Gherkin suite now runs 2 features with 7 scenarios / 37 steps, all passing.
- `cargo test --manifest-path ./bitloops_cli/Cargo.toml --test testlens_sqlite_acceptance`: passed after the command migration.

Updated migration picture
- The surviving Bitloops command surface now covers:
  - `init`
  - `ingest-tests`
  - `ingest-coverage`
  - `ingest-coverage-batch`
  - `ingest-results`
  - `query`
  - `list`
- What still prevents deleting the whole `TestLens` directory is no longer missing CLI coverage for the supported workflow. The remaining work is mainly:
  - moving/retiring the leftover standalone `TestLens` e2e suite and docs
  - deciding where the shared library code should live once the standalone `testlens` binary is removed

2026-03-19

Started the TestLens deletion tranche

Status: in progress

Completed
- Removed the standalone `testlens` binary entrypoint files:
  - `TestLens/src/main.rs`
  - `TestLens/src/cli.rs`
- Replaced the old CLI-owned `QueryViewArg` with a library-owned shared module in `TestLens/src/read/query_view.rs`.
- Updated the shared read/query code and the Bitloops command layer to use the library-owned query view type instead of depending on the removed standalone CLI module.
- Simplified `TestLens/src/app.rs` down to a pure module root, so the crate now behaves as a shared library surface instead of a runnable CLI.
- Retired the top-level standalone integration harness by removing `TestLens/tests/e2e.rs`.
- Refactored production ingestion to expose a reusable library `execute()` path in `TestLens/src/app/commands/ingest_production_artefacts.rs`.
- Replaced Bitloops-side production seeding calls that previously invoked the command-shaped `ingest_production_artefacts::handle(...)`.
  - `bitloops_cli/tests/test_harness_support/mod.rs` now owns `seed_production_artefacts(...)`
  - Bitloops-side acceptance tests call that helper instead of the old command wrapper

Validation
- `cargo test --manifest-path ./TestLens/Cargo.toml --lib`: passed.
- `cargo test --manifest-path ./bitloops_cli/Cargo.toml --test testlens_gherkin`: passed.
- `cargo test --manifest-path ./bitloops_cli/Cargo.toml --test testlens_sqlite_acceptance`: passed.

Current deletion status
- The standalone `testlens` executable surface is effectively removed.
- The `TestLens` directory still cannot be deleted yet because `bitloops_cli` still depends on the shared `testlens` library crate by path.
- The main remaining deletion work is:
  - move the shared library code into `bitloops_cli` or a renamed shared crate
  - clean stale `TestLens` docs/features that still describe the removed standalone CLI

2026-03-19

Completed the TestLens collapse into `bitloops_cli`

Status: done

Completed
- Copied the shared test-harness implementation into `bitloops_cli/src`:
  - `app`
  - `db`
  - `domain`
  - `read`
  - `repository`
- Removed the external `testlens` path dependency from `bitloops_cli/Cargo.toml`.
- Re-exported the internalized modules from the `bitloops` binary root so the shared `commands/` tree compiles for both the library and the binary target.
- Moved reusable acceptance fixtures into `bitloops_cli/tests/fixtures`:
  - `testlens-fixture`
  - `testlens-fixture-rust`
- Updated `bitloops_cli/tests/test_harness_support/mod.rs` so the acceptance suite no longer reads fixtures from the removed `TestLens` tree.
- Rewrote the root `quickstart.md` into a Bitloops-only flow using `bitloops devql` plus `bitloops testlens`.
- Deleted the obsolete `TestLens` directory after the migrated code and fixtures were fully owned by `bitloops_cli`.

Validation
- `cargo test --manifest-path ./bitloops_cli/Cargo.toml --lib`: passed
  - `1514` passed
  - `8` ignored
- `cargo test --manifest-path ./bitloops_cli/Cargo.toml --test testlens_gherkin`: passed
  - `2` features
  - `7` scenarios
  - `37` steps
- `cargo test --manifest-path ./bitloops_cli/Cargo.toml --test testlens_sqlite_acceptance`: passed
  - `2` passed

Current status
- `bitloops_cli` is now the only remaining runtime and code home for the migrated test harness.
- Remaining `TestLens` mentions in repo-level notes are historical migration references only, not live code or fixture dependencies.

2026-03-19

Restored Ruff quickstarts after the `TestLens` deletion

Status: done

Completed
- Added the Ruff Bitloops-first DevQL quickstart at `bitloops_cli/docs/test_harness/quickstart_ruff_bitloops_devql.md`.
- Added the Ruff workspace test-harness quickstart at `bitloops_cli/docs/test_harness/quickstart_ruff_fixture.md`.
- Updated the root `quickstart.md` to link to both Ruff-specific guides.

Notes
- The old in-repo Ruff fixture snapshots were deleted with `TestLens`, so the restored docs now describe a fresh local Ruff clone instead.
- The restored Ruff docs are explicit about the real DevQL boundary: production artefacts are only materialized for files included in committed Bitloops checkpoints.

2026-03-19

Validated the `cfg-if` runbook flow and confirmed production artefact ingest

Status: done

Completed
- Re-ran the root `runbook.md` flow against a fresh disposable clone at `/tmp/cfg-if-runbook`.
- Used the documented shell-only Codex hook cycle to create one committed Bitloops checkpoint before DevQL ingest.
- Verified the pre-ingest checkpoint state in SQLite:
  - `checkpoints=1`
  - `commit_checkpoints=1`
  - `commits=0`
  - `file_state=0`
  - `artefacts_current=0`
- Ran `bitloops devql ingest` with `BITLOOPS_DEVQL_EMBEDDING_PROVIDER=none` and `BITLOOPS_DEVQL_SEMANTIC_PROVIDER=none`.
- Verified the post-ingest production state in SQLite:
  - `commits=1`
  - `file_state=2`
  - `artefacts_current=8`
  - `artefact_edges_current=1`

Notes
- The validated commit was `4e6e4b4a089858f4fbb6745f5e28a9b0cd1cf807`.
- `commit_checkpoints` mapped checkpoint `d5d94263a243` to that commit.
- The ingested file set was the touched checkpoint payload: `src/lib.rs` plus `codex-flow.jsonl`.

2026-03-19

Validated `bitloops testlens ingest-tests` on the checkpoint-scoped `cfg-if` DevQL DB

Status: done

Completed
- Ran `bitloops testlens ingest-tests --commit 4e6e4b4a089858f4fbb6745f5e28a9b0cd1cf807` in `/tmp/cfg-if-runbook`.
- The command succeeded with:
  - `files: 2`
  - `suites: 4`
  - `scenarios: 5`
  - `links: 0`
  - `enumeration: hybrid-full`
  - `enumerated_scenarios: 2`
- Verified test-domain table counts:
  - `test_suites=4`
  - `test_scenarios=5`
  - `test_links=0`
  - `test_discovery_runs=1`
  - `test_discovery_diagnostics=0`

Notes
- The end-to-end handoff from DevQL production rows to test-harness discovery works.
- This particular checkpoint materialized only `src/lib.rs` and `codex-flow.jsonl`, and the visible Rust artefacts in `src/lib.rs` were mostly the inline test module and helper declarations.
- As a result, test discovery succeeded, but there were no production-to-test links to create in this small checkpoint-scoped example.

2026-03-19

Validated a `claude-code` end-to-end proof with non-zero Rust test links

Status: done

Completed
- Created a disposable repo from `bitloops_cli/tests/fixtures/testlens-fixture-rust` at `/tmp/test-harness-claude-proof`.
- Initialized Bitloops with `bitloops init --agent claude-code`, `bitloops enable --project`, and `bitloops devql init`.
- Drove two real `claude-code` hook sessions from the shell:
  - session 1 added `UserRepository::has_email_domain` and committed checkpoint `93dcb6cbe4d3` mapped to commit `bb915666e361695ba9d861cff54ba78d2e13aee9`
  - session 2 added the `checks_email_domain` test and a doc comment touch on the production method, then committed checkpoint `3eab48741543` mapped to commit `4f2419e4b69ba255ca3e2a52680b1966453f252d`
- Verified after the first DevQL ingest that `artefacts_current` for `COMMIT_A` contained:
  - `src/repositories/user_repository.rs::impl@8::has_email_domain`
- Re-ran DevQL ingest on `COMMIT_B`, then ran `cargo test` and `bitloops testlens ingest-tests --commit 4f2419e4b69ba255ca3e2a52680b1966453f252d`.
- Verified final test-harness results for `COMMIT_B`:
  - `test_suites=4`
  - `test_scenarios=5`
  - `test_links=9`
- Verified the direct production-to-test link:
  - `checks_email_domain -> src/repositories/user_repository.rs::impl@8::has_email_domain`
- Updated `runbook.md` with the exact validated flow.

Notes
- This is the clean proof that the migrated test harness works on top of checkpoint-scoped DevQL production artefacts when the final checkpoint includes both the production file and the test file.
- The earlier `cfg-if` run showed why this matters: discovery can succeed with `links=0` when the materialized production surface is too small or too test-centric.

2026-03-19

Removed the runtime-side prototype production-ingest helper

Status: done

Completed
- Deleted `bitloops_cli/src/app/commands/ingest_production_artefacts.rs` from the runtime tree.
- Moved the arbitrary workspace production seeding logic into `bitloops_cli/tests/test_harness_support/production_seed.rs`.
- Updated `bitloops_cli/tests/test_harness_support/mod.rs` so acceptance tests keep calling `seed_production_artefacts(...)`, but now through test-only support.
- Added `bitloops_cli/tests/production_seed.rs` and preserved the three important regressions:
  - mixed TypeScript/Rust production artefact discovery ignores `tests/`
  - rerun replaces stale production artefacts for the same commit
  - distinct Rust trait impl methods with the same name do not collide

Validation
- `cargo test --manifest-path ./bitloops_cli/Cargo.toml --test production_seed`: passed
  - `3` passed
- `cargo test --manifest-path ./bitloops_cli/Cargo.toml --test testlens_sqlite_acceptance`: passed
  - `2` passed
- `cargo test --manifest-path ./bitloops_cli/Cargo.toml --test testlens_gherkin`: passed
  - `2` features
  - `7` scenarios
  - `37` steps

Notes
- This removes the last live runtime home for the old `ingest-production-artefacts` prototype path.
- Real production ingest remains exclusively in `bitloops devql ingest`.

2026-03-19

Refactored oversized SQLite and Postgres test-harness backends into smaller modules

Status: done

Completed
- Replaced the monolithic SQLite backend file with:
  - `bitloops_cli/src/repository/sqlite/mod.rs`
  - `bitloops_cli/src/repository/sqlite/writes.rs`
  - `bitloops_cli/src/repository/sqlite/lists.rs`
  - `bitloops_cli/src/repository/sqlite/tests.rs`
- Replaced the monolithic Postgres backend file with:
  - `bitloops_cli/src/engine/test_harness/postgres/mod.rs`
  - `bitloops_cli/src/engine/test_harness/postgres/helpers.rs`
- Kept the public repository types and trait implementations in the backend `mod.rs` files, and moved persistence/query helpers into private submodules so future growth does not collapse back into one file.
- Brought both offending files back under the push guard:
  - `bitloops_cli/src/repository/sqlite/mod.rs = 768 lines`
  - `bitloops_cli/src/engine/test_harness/postgres/mod.rs = 801 lines`

Validation
- `cargo test --manifest-path ./bitloops_cli/Cargo.toml --lib`: passed
  - `1511` passed
- `cargo test --manifest-path ./bitloops_cli/Cargo.toml --test testlens_sqlite_acceptance`: passed
  - `2` passed
- `cargo test --manifest-path ./bitloops_cli/Cargo.toml --test testlens_gherkin`: passed
  - `2` features
  - `7` scenarios
  - `37` steps

Notes
- The refactor is architectural, not behavioral: command/test-harness behavior stayed the same while backend responsibilities became explicit.
- Future backend growth should land in helper modules rather than re-expanding `mod.rs` past the repository line-count guard.

2026-03-19

Fixed the post-refactor Clippy gate for `bitloops_cli`

Status: done

Completed
- Replaced ad-hoc `from_str(...)` helpers on `ScopeKind` and `CoverageFormat` with `std::str::FromStr` implementations in `bitloops_cli/src/domain/mod.rs`.
- Removed the dead `line_count` field from discovered test files and cleaned up the leftover helper in `bitloops_cli/src/app/test_mapping/file_discovery.rs`.
- Refactored `bitloops_cli/src/app/test_mapping/materialize.rs` so the materialization path uses a shared `MaterializationContext` plus small record-spec structs instead of 10–12 positional parameters.
- Reduced `ingest_coverage` command-side argument fan-out by introducing `IngestCoverageRequest` in `bitloops_cli/src/app/commands/ingest_coverage.rs`.
- Cleaned the remaining test-target Clippy failures in test support:
  - converted `map(...).flatten()` to `filter_map(...)`
  - replaced the SQLite repository test batch helper with a small input struct
  - scoped `#[allow(clippy::too_many_arguments)]` to the two large test-only production seeding helpers

Validation
- `cargo clippy --manifest-path ./bitloops_cli/Cargo.toml --all-targets -- -D warnings`: passed
- `cargo test --manifest-path ./bitloops_cli/Cargo.toml --lib`: passed
  - `1511` passed
- `cargo test --manifest-path ./bitloops_cli/Cargo.toml --test testlens_sqlite_acceptance`: passed
  - `2` passed
- `cargo test --manifest-path ./bitloops_cli/Cargo.toml --test testlens_gherkin`: passed
  - `2` features
  - `7` scenarios
  - `37` steps

Notes
- The original backend split was correct, but the repo’s real push gate is `cargo clippy --all-targets -D warnings`, not just `cargo test`.
- Mainline code now follows the stricter shape without blanket lint suppression; the only targeted lint allowances left are in test-only fixture seed helpers.

2026-03-19

Raised `bitloops_cli` coverage above the develop baseline without touching the baseline file

Status: done

Completed
- Added direct Postgres test-harness repository coverage in `bitloops_cli/src/engine/test_harness/postgres/tests.rs`.
  - The new test boots a temporary local Postgres cluster, initializes DevQL plus test-harness schemas, seeds minimal production rows, runs test-discovery/runtime-signal writes through the real repository, and verifies query/read behavior plus expected error paths.
- Fixed several real Postgres-path issues that the previous coverage gap had hidden:
  - Postgres schema strings were using `DATETIME`; updated Postgres-only DDL to `TIMESTAMPTZ` in:
    - `bitloops_cli/src/engine/devql/ingestion/schema/relational_postgres_schema.rs`
    - `bitloops_cli/src/engine/devql/ingestion/schema/relational_postgres_migrations.rs`
    - `bitloops_cli/src/engine/devql/ingestion/semantic_features_persistence.rs`
    - `bitloops_cli/src/engine/devql/ingestion/semantic_embeddings_persistence.rs`
    - `bitloops_cli/src/engine/test_harness/schema.rs`
  - Postgres test-domain integer columns now use `BIGINT` where the shared domain writes `i64`.
  - Postgres repository reads now use explicit `i64` helpers for `INTEGER`/`BIGINT` compatibility in:
    - `bitloops_cli/src/engine/test_harness/postgres/helpers.rs`
    - `bitloops_cli/src/engine/test_harness/postgres/mod.rs`
  - Fixed Postgres-specific `SELECT DISTINCT ... ORDER BY ...` query shapes in the Postgres repository so they do not depend on SQLite behavior.
- Kept the existing migrated suites green after the Postgres fixes.

Validation
- `cargo test --manifest-path ./bitloops_cli/Cargo.toml postgres_repository_round_trips_test_harness_flow --lib`: passed
- `cargo clippy --manifest-path ./bitloops_cli/Cargo.toml --all-targets -- -D warnings`: passed
- `cargo test --manifest-path ./bitloops_cli/Cargo.toml --test testlens_sqlite_acceptance`: passed
  - `2` passed
- `cargo test --manifest-path ./bitloops_cli/Cargo.toml --test testlens_gherkin`: passed
  - `2` features
  - `7` scenarios
  - `37` steps
- `bash bitloops_cli/scripts/coverage-baseline-check.sh check`: passed
  - baseline lines `82.17%` -> current lines `82.95%`
  - baseline functions `75.67%` -> current functions `76.75%`

Notes
- The coverage regression was mainly caused by the newly migrated Postgres backend having effectively `0%` direct execution coverage.
- The fix was not just “add tests”; it also exposed and corrected real Postgres compatibility bugs in schema typing, numeric decoding, and query legality.
